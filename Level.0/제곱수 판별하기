class Solution {
    public int solution(int n) {
        int answer = 2;
        if((Math.sqrt(n)*10) %10 ==0 ) {
            answer = 1;
        }
        return answer;
    }
}

// 원래 for문을 돌면서 i*i=n 이되면 answer=1 을 주려했는데, 잘 안돼서 생각해낸 방법이 제곱근을 사용할 수도 있지 않을까 했다.
// 찾아보니까 Math.sqrt() 메소드가 있어서 사용했다.
