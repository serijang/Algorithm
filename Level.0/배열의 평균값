// numbers_len은 배열 numbers의 길이입니다.
double solution(int numbers[], numbers_len) {
    double answer = 0;
    for(int i=0; i<=numbers_len-1; i++) {
        answer += numbers[i];
    }
    return answer/numbers_len;
}
