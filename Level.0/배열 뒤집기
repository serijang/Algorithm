class Solution {
    public int[] solution(int[] num_list) {

        int len = num_list.length;
        int[] answer = new int[len];  // answer배열의 길이를 잡아줘야함
        for(int i = 0; i <= len-1; i++) {
            answer[i] = num_list[len-1-i];
        }
        return answer;
    }
}
